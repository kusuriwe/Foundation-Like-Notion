import { defaultPresentationValue } from "@foundation-like-notion/contracts/presentation-default"

/** Re-export the Zod-free presentation fallback for browser runtimes. / browser runtime用Zod非依存presentation fallbackを再公開します。 */
export const defaultPresentation = defaultPresentationValue
