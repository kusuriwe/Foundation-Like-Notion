# Generate this value with: docker compose run --rm app npm run auth:hash --workspace @foundation-like-notion/api
READER_PASSWORD_HASH=
# Required only when reader.yaml uses source: notion.
NOTION_TOKEN=
